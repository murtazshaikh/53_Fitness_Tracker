# Host port is 5433 because a native Postgres already listens on 5432 on this machine.
DATABASE_URL="postgresql://fitness:fitness@localhost:5433/fitness?schema=public"
AUTH_SECRET="generate-with-bunx-auth-secret"
