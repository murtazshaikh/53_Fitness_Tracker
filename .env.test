# Tests run against a separate database on the same container, so they can wipe
# tables freely without destroying seeded dev data. No secrets here.
DATABASE_URL="postgresql://fitness:fitness@localhost:5433/fitness_test?schema=public"
AUTH_SECRET="test-only-secret-not-used-for-anything-real"
